name: RBAC Schema Definition

namespaces:
  - batch_changes:
    name: "BATCHCHANGES"
    actions:
      - READ
      - WRITE
      - EXECUTE

  - notebooks:
    name: "NOTEBOOKS"
    actions:
      - READ
      - WRITE

  - codeinsights:
    name: "CODEINSIGHTS"
    actions:
      - READ
      - WRITE
